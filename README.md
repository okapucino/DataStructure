# Data Structures

This repository includes various data structures implementations and algorithms.

## Array

An array is a collection of items stored at contiguous memory locations. The idea is to store multiple items of the same type together, so that we can easily access them using the index of the items.

### Operations
- Access: O(1)
- Search: O(n)
- Insertion: O(n)
- Deletion: O(n)